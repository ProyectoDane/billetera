import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  form: {
    alignContent: 'center',
    justifyContent: 'center',
    paddingHorizontal: '5%',
  },
  title: {
    textAlign: 'center',
    fontWeight: 'bold',
    fontSize: 18,
    paddingBottom: 30,
    paddingTop: 60,
  },
});
