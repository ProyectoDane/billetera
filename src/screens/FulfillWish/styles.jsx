import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    textAlign: 'center',
    fontWeight: 'bold',
    fontSize: 18,
    paddingVertical: 30,
  },
  subtitle: {
    textAlign: 'center',
    fontWeight: 'bold',
    fontSize: 18,
  },
});
